name: Build Electron

on:
  push:
    branches: [main]
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  linux:
    name: Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.11'
      - name: Build server binary
        run: |
          cd server
          pip install -r requirements.txt pyinstaller
          pyinstaller run_server.py \
            --name clawchat-server \
            --onedir \
            --noconfirm \
            --hidden-import aiosqlite \
            --hidden-import sqlalchemy.dialects.sqlite \
            --collect-submodules uvicorn \
            --collect-submodules jose \
            --paths .
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build:electron
      - name: Build Electron package
        run: npx electron-builder --linux --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload AppImage
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ClawChat-linux
          path: release/*.AppImage
      - name: Attach to Nightly Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: nightly
          name: 'Nightly Build'
          prerelease: true
          files: release/*.AppImage

  windows:
    name: Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.11'
      - name: Build server binary
        run: |
          cd server
          pip install -r requirements.txt pyinstaller
          pyinstaller run_server.py --name clawchat-server --onedir --noconfirm --hidden-import aiosqlite --hidden-import sqlalchemy.dialects.sqlite --collect-submodules uvicorn --collect-submodules jose --paths .
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build:electron
      - name: Build Electron package
        run: npx electron-builder --win --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload installer
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ClawChat-windows-installer
          path: release/*Setup*.exe
      - name: Upload portable
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ClawChat-windows-portable
          path: release/*portable*.exe
      - name: Attach to Nightly Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: nightly
          name: 'Nightly Build'
          prerelease: true
          files: |
            release/*Setup*.exe
            release/*portable*.exe

  mac:
    name: macOS
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.11'
      - name: Build server binary
        run: |
          cd server
          pip install -r requirements.txt pyinstaller
          pyinstaller run_server.py \
            --name clawchat-server \
            --onedir \
            --noconfirm \
            --hidden-import aiosqlite \
            --hidden-import sqlalchemy.dialects.sqlite \
            --collect-submodules uvicorn \
            --collect-submodules jose \
            --paths .
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build:electron
      - name: Build Electron package
        run: npx electron-builder --mac --publish always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
      - name: Remove quarantine for ad-hoc builds
        run: |
          for f in release/*.dmg; do
            xattr -cr "$f" 2>/dev/null || true
          done
      - name: Upload DMG
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: ClawChat-mac
          path: release/*.dmg
      - name: Attach to Nightly Release
        if: github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
        with:
          tag_name: nightly
          name: 'Nightly Build'
          prerelease: true
          files: release/*.dmg
